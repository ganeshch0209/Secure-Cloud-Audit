# Core Azure SDKs
azure-identity
azure-mgmt-resource
azure-mgmt-network
azure-mgmt-compute
azure-mgmt-storage
azure-mgmt-keyvault
azure-graphrbac
azure-mgmt-monitor

# CLI & subprocess (optional for CLI integration)
pyyaml
subprocess.run ; python_version >= "3.5"

# CSV & Reporting
pandas

# Optional: For dashboards (if using Streamlit)
streamlit

# Optional: For email alerts
sendgrid

# Optional: For working with config files
python-dotenv

# For clean async or logging (optional but recommended)
loguru
